{{/*
Universal entrypoint supporting single- and multi-app modes.
*/}}
{{- if .Values.apps -}}
{{- /* MULTI APP MODE */ -}}
{{- range .Values.apps }}
{{ include "base.configmap" (dict "root" $ "app" .) }}
---

{{- end }}

{{- else if or .Values.global.configmap .Values.configmap }}
{{- /* SINGLE APP MODE (BACKWARD COMPATIBLE) */ -}}
{{ include "base.configmap" (dict "root" . "app" .Values) }}

{{- end }}